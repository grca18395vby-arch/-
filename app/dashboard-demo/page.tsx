import type { Metadata } from "next";
import { Activity, ArrowUpRight, CreditCard, KeyRound, LifeBuoy, MessageSquare, MoreHorizontal, Server, ShieldCheck, Smartphone, Wallet } from "lucide-react";
import { DEMO_NOTICE } from "@/lib/constants";

export const metadata: Metadata = { title: "控制台演示", description: "深度寻知用户控制台的演示页面，所有数据均为演示数据。" };
const cards = [[Wallet, "当前余额", "¥ 2,480.00", "演示余额"], [Activity, "API 调用次数", "12,480", "近 30 天 · 演示"], [CreditCard, "Token 使用量", "2.4M", "近 30 天 · 演示"]] as const;
const rows = [["POST /v1/chat/completions", "example-model", "200", "刚刚"], ["POST /v1/embeddings", "example-vector", "200", "12 分钟前"], ["GET /v1/status", "gateway", "200", "31 分钟前"]];
export default function DashboardPage() { return <div className="dashboard-page"><div className="dashboard-topbar"><div className="container dash-nav"><span className="brand"><span className="brand-mark"><span /></span><span><strong>深度寻知</strong><small>CONTROL PLANE / DEMO</small></span></span><div><span className="dash-demo-label">DEMO DATA ONLY</span><span className="dash-avatar">D</span></div></div></div><div className="container dashboard-shell"><aside className="dashboard-sidebar"><div className="dash-workspace"><span className="dash-avatar blue">D</span><div><strong>演示工作区</strong><small>个人用户 · demo</small></div><MoreHorizontal size={16} /></div>{[[Activity,"概览"],[KeyRound,"API 密钥"],[CreditCard,"账单记录"],[Server,"部署服务"],[Smartphone,"短信订单"],[LifeBuoy,"工单管理"]].map(([Icon, label], index) => <div className={`dash-menu ${index === 0 ? "active" : ""}`} key={label as string}><Icon size={16} />{label as string}</div>)}<div className="dash-side-bottom"><ShieldCheck size={15} /><span>所有数据均为<br />官网演示内容</span></div></aside><main className="dashboard-main"><div className="dashboard-heading"><div><span className="eyebrow">OVERVIEW / DEMO</span><h1>工作区概览</h1><p>{DEMO_NOTICE}</p></div><button className="button button-secondary">导出记录 <ArrowUpRight size={15} /></button></div><div className="dash-metrics">{cards.map(([Icon, label, value, note]) => <div className="dash-metric" key={label as string}><div><span className="dash-metric-icon"><Icon size={17} /></span><span>{label as string}</span></div><strong>{value as string}</strong><small>{note as string}</small></div>)}</div><div className="dashboard-grid"><section className="dash-panel requests-panel"><div className="panel-head"><div><span className="eyebrow">RECENT REQUESTS</span><h2>最近请求</h2></div><a href="/docs">查看文档 <ArrowUpRight size={14} /></a></div><div className="request-table"><div className="table-row table-head"><span>接口</span><span>模型</span><span>状态</span><span>时间</span></div>{rows.map((row) => <div className="table-row" key={row[0]}><span>{row[0]}</span><span>{row[1]}</span><span className="status-ok">{row[2]}</span><span>{row[3]}</span></div>)}</div></section><section className="dash-panel orders-panel"><div className="panel-head"><div><span className="eyebrow">SERVICE ORDERS</span><h2>服务订单</h2></div><MoreHorizontal size={16} /></div>{[["AI API 服务咨询","评估中","blue"],["部署服务需求","待确认","amber"],["Codex Skill 安装","已完成","green"]].map(([name, status, tone]) => <div className="order-row" key={name}><span className={`order-icon ${tone}`}><Server size={15} /></span><div><strong>{name}</strong><small>演示订单 · 2026-07-15</small></div><span className={`order-status ${tone}`}>{status}</span></div>)}</section></div><div className="dashboard-bottom-grid"><div className="dash-panel progress-panel"><div className="panel-head"><div><span className="eyebrow">DEPLOYMENT PROGRESS</span><h2>部署服务进度</h2></div><span className="demo-badge">DEMO</span></div><div className="progress-steps">{["需求评估", "环境规划", "服务部署", "运行测试"].map((item, index) => <div className={index < 2 ? "complete" : ""} key={item}><span>{index < 2 ? "✓" : `0${index + 1}`}</span><strong>{item}</strong></div>)}</div></div><div className="dash-panel support-panel"><span className="support-icon"><MessageSquare size={18} /></span><div><span className="eyebrow">NEED HELP?</span><h2>需要协助？</h2><p>提交一个新的需求或查看帮助中心。</p><a href="/contact">联系服务团队 <ArrowUpRight size={14} /></a></div></div></div></main></div></div>; }
